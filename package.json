{
  "name": "lunad",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @lunad/web",
    "build": "npm run build --workspace @lunad/web",
    "typecheck": "npm run typecheck --workspace @lunad/web"
  }
}